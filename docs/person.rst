person module
=============

Typisches Anwendungsbeispiel:

| person.init()
| auth = auth(RFID)
| check = addPerson(String, RFID)
| check = rfidExists(RFID)
| check = nameExists(String)
| name = getName(RFID)
| rfid = getRFID(String)
| date = lastSeen(RFID) 

.. automodule:: person
   :members:
   :undoc-members:
   :show-inheritance:
